// import * as types from '../mutation-types'

const state = {
}

// mutations
const mutations = {
	// UPDATE_SEATS (state, val) {
	// 	state.laptop = val
	// },
}

const actions = {
}

export default {
	state,
	mutations,
	actions,
}

